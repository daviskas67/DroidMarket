warchdog

while ($true) {
    Write-Host "[$(Get-Date)] Starting tunnel..."
    ssh -R 80:127.0.0.1:5000 -N -o "ServerAliveInterval=30" -o "ServerAliveCountMax=5" -o "ExitOnForwardFailure=yes" barbaros.serveousercontent.com
    
    $exitCode = $LASTEXITCODE
    Write-Host "[$(Get-Date)] Tunnel died with code $exitCode. Restarting in 3 seconds..."
    Start-Sleep -Seconds 3
}